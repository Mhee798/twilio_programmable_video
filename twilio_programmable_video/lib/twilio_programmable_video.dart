/// Twilio Programmable Video utility package.
///
/// This is a preliminary API.
// The library name is load-bearing: dartdoc_options.yaml selects what to
// document with `include: ['twilio_programmable_video']`, which matches on the
// library name. src/parts.dart is deliberately left unnamed so this stays the
// only library with this name (it used to be declared in both, which made
// `part of twilio_programmable_video;` ambiguous).
// ignore: unnecessary_library_name
library twilio_programmable_video;

export 'src/parts.dart';
